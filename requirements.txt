# Core engine (Phase 1-3) -- pure Python, no UI dependencies
pydantic>=2.7
rapidfuzz>=3.9
networkx>=3.3
python-dotenv>=1.0

# Optional LLM explanations (Phase 3) -- everything runs without an API key
anthropic>=0.40

# Portal (Phase 4-6)
dash>=2.17,<3
plotly>=5.22
dash-mantine-components>=0.14
dash-cytoscape>=1.0

# Tests (Phase 2)
pytest>=8.2
