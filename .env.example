# Optional. Enables LLM-generated explanations (Phase 3).
# Leave empty (or delete .env entirely): the app falls back to deterministic
# template explanations and everything still runs.
ANTHROPIC_API_KEY=
