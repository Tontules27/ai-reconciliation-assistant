# Optional. Enables LLM-generated explanations (Phase 3).
# Providers cascade: Anthropic is tried first, then Gemini. Leave both empty
# (or delete .env entirely): the app falls back to deterministic template
# explanations and everything still runs.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
